# Config for local development when running services on the host machine
EXPOSE_PORT_HTTP=8080
EXPOSE_PORT_HTTPS=8443
NGINX_HOSTNAME=localhost
AUTH_HOST=keycloak
# Change the following to the IP address of the host machine
FRONTEND_HOST=192.168.0.110
BACKEND_HOST=192.168.0.110
