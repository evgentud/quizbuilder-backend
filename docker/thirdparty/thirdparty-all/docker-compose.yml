include:
    - ../postgres/docker-compose.yml
    - ../keycloak/docker-compose.yml
    - ../proxy/docker-compose.yml

networks:
  backend_network:
    name: backend_network
    driver: bridge
    external: true